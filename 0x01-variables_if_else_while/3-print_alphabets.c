#include <stdio.h>
/*main-prints alphabets excepts q and e*/
/*return: 0;*/

main(void)
{
	alph = a;
	while (alph < = z)
	{
		while (alph != q && alph != e)
		{
		putchar ("%c," alph);
		}
		putchar ("\n");
	}
return (0);
}
