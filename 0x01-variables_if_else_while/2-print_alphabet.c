#include <stdio.h>
/**
 * main -Prints small alphabetical letters
 *
 * Return: 0;
*/
char main(void)
{
char alph;

while (alph = 'a' alph < = 'z' alph++)
{
putchar ("%c", alph);
}
putchar ("\n");
return (alph);
}
